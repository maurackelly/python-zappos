from setuptools import setup, find_packages

setup(
    name='zappos',
    version='0.1',
    author='Maura Chace',
    url='http://github.com/minarets/python-zappos',
    packages=find_packages(),
)
